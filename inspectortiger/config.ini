[Config inspectortiger]
workers = max

[Plugins inspectortiger.plugins]
context manager = context
unreachable except finder = unreachable_except
syntax improver = upgradeable
misc = misc
parentize = parentize
